import React from 'react';
import { useForm } from 'react-hook-form';

const Form = () => {
  const { register, reset, handleSubmit } = useForm;

  return (
    <form>
      <input />
    </form>
  );
};

export default Form;
